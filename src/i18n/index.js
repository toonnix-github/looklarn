import { th } from './th';
import { en } from './en';

export const DEFAULT_LANGUAGE = 'th';
export const SUPPORTED_LANGUAGES = ['th', 'en'];

export const translations = {
  th,
  en,
};

export { th, en };
